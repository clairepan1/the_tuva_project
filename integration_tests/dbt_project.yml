name: 'integration_tests'
version: '1.0.0'
config-version: 2

profile: 'tuva-snowflake'

vars:

  input_database: dev_aaron
  input_schema: tuva_synthetic

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  +database: dev_aaron3

models:
  integration_tests:
    +materialized: ephemeral
  the_tuva_project:
    +database: claims_data_profiling


  